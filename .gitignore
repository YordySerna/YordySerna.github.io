.claude/

# dev/ son las herramientas locales del rediseño: scripts de captura,
# preparación de la foto y el inventario de proyectos. Generan lo que se
# publica, pero el sitio servido no depende de ellas — GitHub Pages sirve
# HTML, CSS, JS y las imágenes ya procesadas, nada más.
dev/
